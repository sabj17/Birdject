#define pin1 1
#define pin2 2
#define pin3 3
#define pin4 4
#define pin6 6
#define pin7 7
#define pin8 8
#define pin9 9
#define pin10 10
#define pin11 11
#define pin12 12
#define pin13 13

class Output {
  public:
    int pin;
    int state = 0;

    Output(int inputPin) {
      pin = inputPin;
    }
    void setupClass() {
      pinMode(pin, OUTPUT);
    }


    void setState(int newState) {
      state = newState;
      digitalWrite(pin, newState);
    }

    int getState(){
      return state;
    }

    void switchState() {
      if (state) {
        state = 0;
      } else {
        state = 1;
      }
      digitalWrite(pin, state);
      delay(200);
    }
};

class Light : public Output {
  public:
    Light (int inputPin): Output(inputPin) {}
};

class Input {
  public:
    int pin;

    Input (int inputPin) {
      pin = inputPin;
    }

    void setupClass() {
      pinMode(pin, INPUT);
    }

    int Read() {
      return digitalRead(pin);
    }
};


class Switch : public Input {
  public:
    Switch (int inputPin): Input(inputPin) {}
};

Light lightGlobal(pin1);


class LivingRoomClass {
  public:
	Light light;
	Switch switch1;

	type jenerator (type jebat, type jabat){
		int jebat = 5 + 5;
		char jabat[100] = "Anus";
		if (jabat == "Anus") {
				jebat = 2;
		}
		return jebat;
	}

LivingRoomClass() : light(pin6) , switch1(pin7) {}

} LivingRoom;

type tester (type y){
	bool y = true;
	return y;
}

Light lightGlobal2(pin2);


class KitchenClass {
  public:
	Light light2;
	Switch switch2;
KitchenClass() : light2(pin8) , switch2(pin9) {}

} Kitchen;
Light lightGlobal3(pin3);
	int x = 10;
void setup() {
	Serial.begin(9600);	lightGlobal.setupClass();	LivingRoom.light.setupClass();	LivingRoom.switch1.setupClass();	lightGlobal2.setupClass();	Kitchen.light2.setupClass();	Kitchen.switch2.setupClass();	lightGlobal3.setupClass();	Serial.print("Hello World!");
}
void loop() {
	if (	LivingRoom.switch1.Read()){
	LivingRoom.light.switchState();
	}
	if (5 < 10){
	Serial.print("This is true");
	Serial.print(5.5);
	Kitchen.light2.setState(true);
	if (10 == 4) {
			Serial.print("Gottem");
	}
	else 	if (10 == 9) {
			Kitchen.light2.setState(false);
	}
	else {
			Kitchen.switch2.Read();
	}
	}
		if (10 == 10){
		if (		Kitchen.switch2.Read();
) {
				Serial.print("Nou good one");
		}
		}}