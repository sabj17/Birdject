LivingRoom {
    set light1 to Light(pin6);
    set switch1 to Switch(pin7);
    set y to 10;
}

set x to off;

function tester(){
    run LivingRoom.light1.switchState();
}


when (run LivingRoom.switch1.Read()){
    run tester();
    set x to run LivingRoom.light1.getState();

    if(x is on){
        run print("Light is on");
    } else {
        run print("Light is off");
    }
}