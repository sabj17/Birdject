LivingRoom {
    set light1 to Light(pin8);
    set switch1 to Switch(pin2);
}

set x to off;


when (run LivingRoom.switch1.isTurnedOn()){
    set x to run LivingRoom.light1.changeMode();

    if(x is on){
        run print("Light is on");
    } else {
        run print("Light is off");
    }
}