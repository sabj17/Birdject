LivingRoom {
    set window1 to Window(pin8);
    set temp1 to Thermometer(pinA0);
}

when (run LivingRoom.temp1.getTemp() < 22){
    run LivingRoom.window1.setMode(on);
}

when (run LivingRoom.temp1.getTemp() > 22){
    run LivingRoom.window1.setMode(off);
}
