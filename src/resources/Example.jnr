set switch1 to Switch(pin11);
set list1 to List();
set number1 to 4 + 2 * 3 - 2;
set string to "Hello";
set bacon to string;
set superString to "hey" + "baby";
set numberString to "high" + "five";
set numberString to "high" + 5 + bacon;
set input1 to false;


LivingRoom{
    set light to Light(pin9, pin10);
	set temp1 to Thermometer();
	set constant to 5 * (10 + 4) / 2 % 1 - 3;
	set window1 to off;
	set x to 10;

	function getTemp1(n){
        set dab to 1234.2;
        run print("hello");
		return temp1;
	}

	function openWindow(){
	    set clap to 987;
		run getTemp1(clap);
		return "well done";
	}

	function closeWindow(window2){
	    run openWindow();
	    return true;
	}

	AnotherRoom{
	    set variable to Thermometer();

	    function burnRoom(i){
	        return i;
	    }
	}

}

function globalFunc(input1, input2, input3){
    set smil123456 to 6666666;
    set input1 to false;
    set input2 to 1;
    set input3 to 2;
    set a to "h";
    set b to "a";

	if (input1){

	} else if (input2 is input3) {
	    set haha to "fuck you";
        return "hahaha";
	} else if (a + b is "ha") {
	    set yaya to 77;
        return numberString;
	}
	else{
	    return a;
	}

}

when(run LivingRoom.getTemp1(number1) > 20){
    set hello to 2;
    set x to 22;
    set windowXX to 55;
	run LivingRoom.openWindow();
	run LivingRoom.closeWindow(windowXX);
	run globalFunc(x + hello, 99, string);
	run LivingRoom.AnotherRoom.burnRoom(x);
	run switch1.setState(12);
	run LivingRoom.light.switchState();
}